export const controlSpacing = 20;
export const controlIconSize = 35;

export const controlSpacingSmall = 10;
export const controlIconSizeSmall = 20;

export const rangeTrackHeight = 10,
    rangeTrackHeightSmall = 8,
    rangeThumbActiveScale = 1.25,
    rangeThumbSize = Math.floor(rangeTrackHeight * 1.8),
    rangeThumbSizeSmall = Math.floor(rangeTrackHeightSmall * 1.8),
    tooltipArrowSize = 4,
    progressLoadingSize = 25,
    progressLoadingBg = 'rgba(0, 0, 0, .85)';
